akka {
  log-dead-letters = 0
  log-dead-letters-during-shutdown = off

  actor {
    # Enable the local message serialization config to
    # verify the message serialization process in local.
    // serialize-creators = on
    // serialize-messages = on
  }
}
